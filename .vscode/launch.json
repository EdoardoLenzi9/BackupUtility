{    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: backup.py",
            "type": "python",
            "request": "launch",
            "program": "${file}"
        }
    ]
}